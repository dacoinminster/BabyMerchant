'use strict';

// Empty config stub committed to repo to avoid missing script errors in production.
// Use a local untracked js/core/config.js for developer overrides (gitignored).
// Examples:
//   // window.SLOW_TRANSITIONS = true;
//   // window.DEBUG_AFFINE = false;

// var devMode = true;
